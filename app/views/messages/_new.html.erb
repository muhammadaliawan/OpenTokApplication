<%= form_with model: @message do |f| %>
  <div class="input-group mb-3">
    <%= f.text_field :name, placeholder: 'Say something...', class: 'form-control' %>
    <div class="input-group-append">
      <%= f.submit "Send", class: 'btn btn-secondary' %>
    </div>
  </div>
<% end %>
